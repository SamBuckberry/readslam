/*
 * Take a string and build a suffix tree to represent it
 */
#pragma once

namespace Algorithms
{
	
	 SuffixTree() {}
	~SuffixTree() {}
	
	//Clear the tree
	void clear()
	{
		
	}
	
	//Build a suffix tree for the specified string
	void build(string source)
	{
		
	}
	
	//Load the suffix tree from disk
	void load(string infile)
	{
		
	}
	
	//Save the suffix tree to disk
	void save(string outfile)
	{
		
	}
}